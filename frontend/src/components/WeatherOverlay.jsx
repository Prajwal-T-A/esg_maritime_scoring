import React from 'react';

/**
 * WeatherOverlay Component (Legacy - visualization now in LiveMap)
 * This component is kept for reference but main weather visualization
 * is now handled by PrecipitationColorLayer and WindIndicatorLayer in LiveMap.jsx
 */

const WeatherOverlay = () => {
    // This component is no longer used
    // Weather overlays are now implemented directly in LiveMap
    return null;
};

export default WeatherOverlay;
