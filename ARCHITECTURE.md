# System Architecture Diagram

## Overall System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                    (Browser: localhost:3000)                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             │ (GET /api/v1/...)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND (Port 3000)                   │
├─────────────────────────────────────────────────────────────────┤
│  Components:                                                     │
│  • HealthStatus.js      → Backend health indicator              │
│  • VesselLatest.js      → Latest vessel data display            │
│  • VesselHistory.js     → Historical data table                 │
│  • ESGScoreCard.js      → ESG score visualization               │
│                                                                  │
│  Pages:                                                          │
│  • Home.js              → Main application page                 │
│                                                                  │
│  Services:                                                       │
│  • api.js               → Centralized API calls (Axios)         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ REST API calls
                             │ (JSON responses)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                  FASTAPI BACKEND (Port 8000)                    │
├─────────────────────────────────────────────────────────────────┤
│  Endpoints:                                                      │
│  • GET /api/v1/health                                           │
│  • GET /api/v1/vessels/{mmsi}/latest                           │
│  • GET /api/v1/vessels/{mmsi}/history                          │
│  • GET /api/v1/esg/{mmsi}                                      │
│                                                                  │
│  Components:                                                     │
│  • main.py              → App setup, CORS, routing              │
│  • routes.py            → API endpoint handlers                 │
│  • s3_service.py        → S3 data access layer                 │
│  • schemas.py           → Pydantic models for validation        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ boto3 SDK
                             │ (S3 GetObject, ListObjects)
                             ↓
┌─────────────────────────────────────────────────────────────────┐
│                         AWS CLOUD                               │
├─────────────────────────────────────────────────────────────────┤
│  Amazon S3 Bucket: ai-carbon-esg-data-prajwal                  │
│  ├── processed/                                                 │
│  │   └── features/                                             │
│  │       ├── vessel_001.json  ← AIS + ESG data                │
│  │       ├── vessel_002.json                                   │
│  │       └── ...                                               │
│                                                                  │
│  Data Structure:                                                │
│  {                                                              │
│    "mmsi": "419001234",                                        │
│    "speed_knots": 14.5,                                        │
│    "latitude": 12.9716,                                        │
│    "longitude": 77.5946,                                       │
│    "timestamp": "2026-01-06T18:30:00Z",                       │
│    "estimated_co2_kg": 0.29,                                   │
│    "esg_environment_score": 70                                 │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

## Component Interaction Flow

```
USER ACTION: Enter MMSI "419001234" and click "Latest Data"
    ↓
┌────────────────────────────────────────────────┐
│ 1. React Component (Home.js)                   │
│    - User input captured                       │
│    - setMmsi('419001234')                     │
│    - handleFetchLatest() called               │
└────────────┬───────────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────┐
│ 2. API Service (api.js)                        │
│    - apiService.getVesselLatest('419001234')  │
│    - axios.get('/vessels/419001234/latest')   │
└────────────┬───────────────────────────────────┘
             │
             ↓ HTTP GET Request
┌────────────────────────────────────────────────┐
│ 3. FastAPI Route (routes.py)                   │
│    - @router.get("/vessels/{mmsi}/latest")    │
│    - Parameter validation                      │
│    - Call S3 service                           │
└────────────┬───────────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────┐
│ 4. S3 Service (s3_service.py)                  │
│    - List S3 objects with prefix               │
│    - Filter by MMSI                            │
│    - Parse JSON                                │
│    - Sort by timestamp                         │
│    - Return latest record                      │
└────────────┬───────────────────────────────────┘
             │
             ↓ S3 GetObject API call
┌────────────────────────────────────────────────┐
│ 5. AWS S3                                      │
│    - Retrieve object from bucket               │
│    - Return JSON content                       │
└────────────┬───────────────────────────────────┘
             │
             ↓ JSON data
┌────────────────────────────────────────────────┐
│ 6. FastAPI Response                            │
│    - Validate with Pydantic schema             │
│    - Return JSON response                      │
│    - HTTP 200 OK                               │
└────────────┬───────────────────────────────────┘
             │
             ↓ HTTP Response
┌────────────────────────────────────────────────┐
│ 7. API Service (api.js)                        │
│    - Receive response                          │
│    - Return { success: true, data: {...} }     │
└────────────┬───────────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────────┐
│ 8. React Component (Home.js)                   │
│    - setLatestData(result.data)               │
│    - Component re-renders                      │
│    - VesselLatest component displays data      │
│    - ESGScoreCard component displays score     │
└────────────────────────────────────────────────┘
```

## Frontend Component Hierarchy

```
App.js (React Router)
  │
  └── Home.js (Main Page)
        │
        ├── HealthStatus.js
        │     └── Displays: Backend connectivity status
        │
        ├── Input Section
        │     ├── MMSI input field
        │     └── Action buttons (Latest, History, ESG)
        │
        ├── VesselLatest.js
        │     └── Displays: Location, Speed, Timestamp, CO₂, ESG
        │
        ├── VesselHistory.js
        │     └── Displays: Table of historical records
        │
        └── ESGScoreCard.js
              └── Displays: Large ESG score with color coding
```

## State Management in React

```
Home.js Component State:
┌──────────────────────────────────────┐
│ const [mmsi, setMmsi]               │ ← User input (MMSI number)
│ const [latestData, setLatestData]   │ ← Latest vessel record
│ const [historyData, setHistoryData] │ ← Historical records array
│ const [esgData, setEsgData]         │ ← ESG metrics only
│ const [loading, setLoading]         │ ← Loading indicator
│ const [error, setError]             │ ← Error messages
│ const [activeView, setActiveView]   │ ← Which view to show
└──────────────────────────────────────┘

State Updates Trigger Re-renders:
User clicks "Latest Data"
  → setLoading(true)
  → setActiveView('latest')
  → API call
  → setLatestData(response)
  → setLoading(false)
  → Component re-renders with new data
```

## ESG Score Color Coding Logic

```
Input: esg_environment_score (0-100)
              │
              ↓
        ┌─────────────┐
        │  Score >= 80 │ → 🟢 Green
        └─────────────┘    "Excellent"
              │
        ┌─────────────┐
        │ 60 ≤ Score < 80 │ → 🟠 Orange
        └─────────────┘    "Moderate"
              │
        ┌─────────────┐
        │  Score < 60  │ → 🔴 Red
        └─────────────┘    "Needs Improvement"
```

## API Request/Response Examples

### Request: Get Latest Vessel Data

```
GET http://127.0.0.1:8000/api/v1/vessels/419001234/latest
Headers:
  Content-Type: application/json
```

### Response: Success (200 OK)

```json
{
  "mmsi": "419001234",
  "speed_knots": 14.5,
  "latitude": 12.9716,
  "longitude": 77.5946,
  "timestamp": "2026-01-06T18:30:00Z",
  "estimated_co2_kg": 0.29,
  "esg_environment_score": 70
}
```

### Response: Error (404 Not Found)

```json
{
  "error": "Resource not found",
  "detail": "No data found for MMSI: 419001234"
}
```

## File Dependencies

```
Backend Dependencies:
requirements.txt
  ├── fastapi>=0.115.0
  ├── uvicorn[standard]>=0.32.0
  ├── boto3>=1.35.0
  ├── pydantic>=2.10.0
  └── httpx>=0.27.0

Frontend Dependencies:
package.json
  ├── react@^18.2.0
  ├── react-dom@^18.2.0
  ├── react-router-dom@^6.20.0
  ├── axios@^1.6.2
  └── tailwindcss@^3.3.6
```

## Deployment Architecture (Future)

```
┌──────────────────────────────────────────────────┐
│                    USERS                          │
└────────────────────┬─────────────────────────────┘
                     │
                     ↓
┌──────────────────────────────────────────────────┐
│              CloudFront (CDN)                     │
│         (Frontend static files)                   │
└────────────────────┬─────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ↓                         ↓
┌─────────────────┐      ┌─────────────────┐
│   S3 Bucket     │      │   API Gateway   │
│  (React Build)  │      │                 │
└─────────────────┘      └────────┬────────┘
                                  │
                                  ↓
                         ┌─────────────────┐
                         │  AWS Lambda     │
                         │  (FastAPI)      │
                         └────────┬────────┘
                                  │
                                  ↓
                         ┌─────────────────┐
                         │   S3 Bucket     │
                         │  (Data Storage) │
                         └─────────────────┘
```

---

**Legend:**
- `→` : Direct relationship
- `↓` : Data flow direction
- `├──` : Tree structure
- `│` : Continuation
- `└──` : End of branch
